\import Logic
\import Paths

\data Maybe (A : \Type)
  | just A
  | none

\func unwrap {A : \Type} (x : Maybe A) (notNone : Not (x = none)) : A =>
  \case \elim x, \elim notNone \with {
    | just x, _ => x
    | none, notNone => absurd (notNone idp)
  }
